## Summary

Implements the five-phase content and SEO amendments requested by Jag (J&L Security) in his email of 5 May 2026, sourced verbatim from `docs/2026-05-06-client-amendments-content-pack.md`.

- **Phase 1**: Burglar alarm cost article rebuilt around confirmed J&L 2026 pricing (Pyronix Enforcer V11 from £485 + VAT, app/monitoring/maintenance starting prices, two-table summary, 6 FAQs)
- **Phase 2**: 14 new London location pages (Islington, Hackney, Clapton, Dalston, Camden, Southwark, Woolwich, Westminster, Hammersmith, Battersea, Fulham, Streatham, Finchley, Barnet) plus light enrichment to Greenwich
- **Phase 3**: Fire alarm keyword integration across services page, homepage, about page, location FAQs, and FAQs page (BAFE-certified, BS 5839-1, BS 5839-6, fire risk assessments, smoke alarm install/repair)
- **Phase 4**: New blog post `bs5839-1-and-bs5839-6-explained-2026` (~2,150 words, 5 FAQs, complete JSON-LD)
- **Phase 5**: `public/llms.txt` and `public/llms-full.txt` updated with new locations grouped by region, BAFE-certified phrasing, J&L confirmed pricing, BS 5839 article entry, and per-page summaries for the 14 new locations

Also includes one small sitemap fix surfaced during verification (sitemap.ts now sources its location list from `lib/data.ts` rather than a hardcoded array, so the 14 new locations are picked up automatically).

## Files changed (high-level)

- `lib/blog.ts` (Phases 1 + 4): burglar alarm article rewrite + new BS 5839 article
- `lib/data.ts` (Phase 2): 14 new `Location` entries appended
- `app/locations/[location]/page.tsx` (Phases 2 + 3): 14 new `locationExtended` entries, Greenwich enrichment, two new generic FAQs in `locationFaqs`
- `app/services/[service]/page.tsx` (Phase 3): rewritten fire-alarms block (heroTagline, overview, whoFor, pricing), four new FAQs, per-slug `metaOverrides` for the fire-alarms route
- `app/page.tsx` (Phase 3): homepage Fire Alarms card description rewritten
- `app/about/page.tsx` (Phase 3): BAFE accreditation description rewritten to lead with "BAFE-certified fire alarm maintainer (SP203-1 scheme)"
- `app/faqs/page.tsx` (Phase 3): three new FAQ items appended to the Fire Alarms category
- `public/llms.txt` and `public/llms-full.txt` (Phase 5): regional service area groupings, fire alarm description rewrite, J&L confirmed pricing, BS 5839 article entry, per-location summaries
- `app/sitemap.ts` (verification follow-up): location list now sourced from `lib/data.ts`

## FAQ architecture decision

Chose **inline** for the FAQs page changes. The existing `app/faqs/page.tsx` uses a richer category-with-icon structure (Lucide React component icons keyed per category) that does not fit the simple `{id, question, answer, category}` shape exported (and currently unused) from `lib/data.ts`. Migrating would either drop the icons or require an additional grouping layer for marginal benefit. Keeping the categorised FAQ list inline as the single source of truth avoids splitting concerns. The unused `faqs` export in `lib/data.ts` is left in place; cleanup is out of scope for this round.

## Pricing change call-out

The burglar alarm cost article now reflects the confirmed J&L Security 2026 starting prices supplied by Jag, presented with the explicit "from £X + VAT" convention and the standing free-survey caveat. This replaces the earlier indicative residential maintenance range (£80 to £150 per year) with the confirmed J&L starting rates (£120/£180 + VAT per year for residential/commercial). The sector-wide indicative ranges remain in the article alongside the J&L confirmed prices for comparison.

## Verification status (local)

- `npm run build`: green (109 pages, was 108)
- TypeScript: clean
- ESLint: 1 pre-existing warning (`CheckCircle` unused on `app/about/page.tsx`); zero new
- Static params: 29 location slugs (was 15), 9 blog slugs (was 8)
- Sitemap: 100 URLs (was 86), all 14 new location pages plus BS 5839 article present
- JSON-LD: LocalBusiness/ProfessionalService, FAQPage, BreadcrumbList all generate without error on new pages; BlogPosting + FAQPage + BreadcrumbList on the new article
- Manual page checks pass for /blog/burglar-alarm-cost-uk-2026, /blog/bs5839-1-and-bs5839-6-explained-2026, four new locations, /services/fire-alarms, /faqs, /llms.txt
- Both phone numbers visible on every page checked
- All J&L confirmed prices use the "from £X + VAT" convention
- Zero em dashes introduced in any new copy
- BS 5839 hyphenation correct throughout (BS 5839-1, BS 5839-6); zero "BS5838" or "BS5839" without hyphen
- BAFE references consistently "BAFE certified" or "BAFE-certified fire alarm maintainer"
- UK English throughout new copy

## Vercel preview URL

Vercel will populate the preview URL on this PR automatically. Preview verification will follow on the deployed URL before requesting merge.

## Open follow-ups

- `serviceLocationMatrix` extension to add high-intent service-location combos for the 14 new London locations (e.g. `burglar-alarm-installation-islington`, `cctv-installation-hammersmith`, `fire-alarm-installation-barnet`). Flagged in the content pack as a deferred item; tracked as a separate later commit.
- One pre-existing ESLint warning in `app/about/page.tsx` (`CheckCircle` unused). Not introduced here; cleanup deferred.

## Test plan

- [ ] Vercel preview deploy completes successfully
- [ ] /blog/burglar-alarm-cost-uk-2026 renders new pricing block, table, both phone numbers, "from £485 + VAT" anchor
- [ ] /blog/bs5839-1-and-bs5839-6-explained-2026 renders, internal links resolve, both phone numbers present
- [ ] /locations/islington, /locations/hammersmith, /locations/barnet, /locations/woolwich render with hero, description, FAQs, CTA
- [ ] /locations/greenwich shows the lightly enriched whyLocal copy (BAFE-certified, Royal Arsenal Riverside, Kidbrooke Village)
- [ ] /services/fire-alarms shows the BAFE-certified hero tagline, updated overview, four new FAQs
- [ ] /faqs shows the three new fire-alarm FAQs
- [ ] /llms.txt is reachable on the preview and shows the new regional groupings, J&L pricing, BS 5839 article entry

🤖 Generated with [Claude Code](https://claude.com/claude-code)
